import React from 'react'
import './Button.module.css'
import cx from 'classname'

const Button = props => {
  return <div styleName="btn">Button</div>
}

Button.propTypes = {}

export default Button
